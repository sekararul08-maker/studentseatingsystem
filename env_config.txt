# ============================================================================
# ENV / KEY=VALUE CONFIG FORMAT — not JSON, YAML, or a programming language;
# its own plain key=value text format. Only file of this kind: .env.example
# ============================================================================

# ==== FILE: .env.example (project root) ====
# ============================================================================
# Smart Examination Hall Seating Allocation System — environment variables
# Copy this file to backend/.env and fill in real values.
# NEVER commit the real .env file.
# ============================================================================

# PostgreSQL connection string used by Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smart_exam_db?schema=public

# JWT
JWT_SECRET=replace_this_with_a_long_random_string
JWT_EXPIRES_IN=8h

# Server
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# File upload limits
MAX_UPLOAD_SIZE_MB=10

# Rate limiting (login endpoints)
LOGIN_RATE_LIMIT_WINDOW_MS=900000
LOGIN_RATE_LIMIT_MAX=10
